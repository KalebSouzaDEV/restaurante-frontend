import { DecimalCommaPipe } from './decimal-comma.pipe';

describe('DecimalCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
